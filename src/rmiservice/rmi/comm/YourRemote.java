package rmiservice.rmi.comm;
/**
 * YourRemote is the interface that needs to be implemented if a service wants to be bound to the 
 * registry. Hence, every client interface of a remote service needs to extend this interface.
 */

public interface YourRemote {
	 
}
